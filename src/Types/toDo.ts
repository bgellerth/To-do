export type Todotype = {
  text: string;
  isEditing: boolean;
  isPriorityChanged: boolean;
  priority: number;
  title: string;
  isChecked: boolean;
  date: string;
  sort: string;
  index: number;
  id: string;
};
